version: '3.4'
services:
  php:
    image: "${KATA_DOCKER_REGISTRY:-docker-repository.intern.neusta.de}/teamneusta/remote-php-kata:8.2"
    volumes:
    - ./:/app
    environment:
      XDEBUG_MODE: debug
    expose:
      - 9003 # xdebug 3 port!
